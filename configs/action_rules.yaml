meta_action_v0:
  label_rule_version: "phase-1.6-meta-action-v0.2"
  horizon_sec: 3.0
  sample_interval_sec: 0.5
  stop_distance_threshold_m: 0.5
  lateral_displacement_threshold_m: 1.0
  forward_displacement_threshold_m: 3.0
  speed_action_min_forward_displacement_m: 1.0
  speed_change_threshold_mps: 1.0
  boundary_margin: 0.2
  all_zero_tolerance_m: 0.001
  coordinates:
    frame: "ego"
    x_axis: "forward"
    y_axis: "left"
    unit: "meter"

# Meta-action v0.2 passed the Phase -1.8 108-sample regression and Phase -1.9
# freeze gate; it is frozen for Phase 0. These project data rules are not
# nuScenes ground truth or an industry standard.
